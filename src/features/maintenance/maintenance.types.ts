export interface FeaturedItem {
  category: string;
  title: string;
  text: string;
  img: string;
}

export interface CategoryItem {
  title: string;
  text: string;
  img: string;
}

export interface SupportItem {
  title: string;
  text: string;
  img: string;
}
